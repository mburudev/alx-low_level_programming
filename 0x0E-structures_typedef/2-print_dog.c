#include <stdio.h>
#include "dog.h"

/**
 * print_dog - prints a struct dog.
 * @d: pointer
 * Return: nothing.
 */

void print_dog(struct dog *d)
{
	if (name || age || owner == NULL)
	{
		printf("Name: (nil)\n");
	}
}
