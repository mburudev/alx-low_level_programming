#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count.
 * @argv: argument vector.
 * Return: 0 if no number and 1 if error.
 */

int main(int argc, char *argv[])
{
	if (argc = 1)
	{
	printf("0\n");
	}
}
