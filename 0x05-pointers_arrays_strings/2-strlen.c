#include<stdio.h>
#include "main.h"

/**
 * _strlen(char *s) - check description
 * Description: Returns the length of a string.
 * @s: pointer for the string
 * Return: 0
 */

int _strlen(char *s)
{
	strlen(*s);
}
