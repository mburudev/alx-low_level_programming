#include "main.h"

/**
 *_abs - returns the absolute value of a number
 * @r: An integer input
 * Return: The absolute value of number r
 */
int _abs(int r)
{
	if (r < 0)
	return (-r);
	else
	return (r);
}
